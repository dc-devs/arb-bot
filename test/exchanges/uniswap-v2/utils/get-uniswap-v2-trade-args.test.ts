import chai from 'chai';
import dotenv from 'dotenv';
// import getUniswapV2TradeArgs from '../../../../src/exchanges/uniswap-v2/get-uniswap-v2-trade-data';

const { expect } = chai;

before(() => {
	dotenv.config();
});

describe('getUniswapV2TradeArgs', async () => {
	xit('should return correct trade data', async () => {
		expect(true).to.equal(false);
	});
});
